import AuthLayout from "@/layouts/auth-layout";
import { Head } from "@inertiajs/react";

export default function Supplier() {
    return (
        <div>
        </div>
    );
}

Supplier.layout = (page: any) => <AuthLayout page={page} title="Supplier Login | Agua Na Casa" header="Login"></AuthLayout>